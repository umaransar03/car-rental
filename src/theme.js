import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
        primary: {
            main: '#c3073f'
        },
        secondary: {
            main: '#000000'
        },
        
    }
});

export default theme;